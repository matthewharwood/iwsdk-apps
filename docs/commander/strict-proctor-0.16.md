# Entry-caused trigger tax, development 0.16

This increment implements Strict Proctor's complete pinned body: a flying 1/3 Spirit Cleric that counters an ability caused by a permanent entering unless that ability's controller pays {2}. The development release has 1,193 primary definitions, 20 separate token templates and 53 legal decks. All 1,192 previous definitions, 20 templates and 51 deck revisions remain exact. Another 30,636 paper candidates remain unsupported.

The release is `13f3654940711c24cd84ff2665d126644687a536749d098f92dea2e52db957e4`, with `commander-engine/0.16.0` and `development-match-plan/12`. The [machine-readable evidence](development-0.16-evidence.json) joins source records, rules, execution captures, independent reviews and retained artifacts. The [source expansion](source-strict-proctor-v0.16.json) records admission separately from execution.

Entry-trigger placement now supports two successive active-player/nonactive-player ordering passes. Ordinary entry abilities enter the stack first; Proctor abilities caused by their triggering enter above them afterward. Each Proctor ability retains the exact lower ability's identity, source, program and captured controller. It does not rediscover a similar current ability. Resolution creates a durable choice owned by that lower ability's controller, including an explicit mana-payment window. Paying preserves the referenced ability; declining counters that exact noncard stack object if it still exists.

An absent lower ability does not erase the later optional payment. The ordinary source history demonstrates a lower ability being countered by one Proctor occurrence, followed by payment for a different occurrence referring to the same absent ability. Mana activated in an earlier accepted command persists through a later decline. Invalid payment commands remain atomic. The terminal displays the owned payment and immutable lower reference through the same validated coordinator.

Source review authenticated 11 complete bodies, 34 rulings and 74 full rule nodes. The focused author capture passed 54 tests with 698 assertions: 25 authenticated-source tests, 27 separately labeled synthetic operator tests and two contract tests. Independent runtime review passed 81 tests with 1,985 assertions, including 27 newly authored tests. These selected-hand and board scenarios are partial histories, not completed games. They cover insufficient mana, exact lower identity and actual Repulse removal of an artifact before a conditional ability's later resolution. Supplemental source contrasts that remain classification-only are identified in the evidence.

The native durability fixture starts from legal 100-card decks and an ordinary deal. A retained seed search selects seed 5241. Two Soul Wardens, two Strict Proctors and two entering Vampire tokens create four ordinary and eight Proctor abilities. All three resolver modes execute the same 402 commands. Ten checkpoints cover both ordering passes, payment, standalone mana, decline and later payment for the absent reference. Reopen, import, replay, duplicate receipts, injected transaction failures and tampered checkpoints passed 27 tests with 1,497 assertions. This six-definition fixture remains ongoing and adds no completed games.

Real Chromium workers replay those same native tapes with SQLite WASM OPFS. All 30 reload checkpoints, 30 separate imports and 60 receipt retries matched native evidence. Twenty-seven imports advance; three already contain the final stopped boundary. No completed game is inferred from this stopped-history proof.

Two selected complete CLI assignments finish in 889 and 3,137 commands. Full-scan, prepared-scan and prepared-indexed independently agree on commands, events, entitled observations, every state boundary and replay. The four-seat game includes 12 paid Proctor resolutions and two declines that counter their referenced abilities.

Selected complete browser games finish in 886 and 2,534 commands, matching native boundaries. Both preserve and pay the same pending Proctor occurrence across reload and import. Their six reload checkpoints include four prior-command retry checkpoints and two separate payment imports; initial revision-zero checkpoints have no prior command to retry. The four-seat game also contains 12 declines and corresponding lower counters. Together, the selected CLI/browser runs represent four execution assignments, 7,446 commands and three initial deck/game-seed families. Resolver variants, namespace changes and replay do not increase that family count.

The [fixed regression](fixed-regression-0.16-evidence.json) separately completes and replays 64 older-deck assignments with 89,784 commands. It retains its actual earlier build pin; only terminal presentation changed before the final selected CLI build. Those fixed decks do not exercise Proctor. Failed and superseded qualification attempts remain retained. Main source and browser worker bytes match their final proof captures.

The full repository gate passes 1,254 unit tests with 137,579 assertions, 12 browser tests, the emulated Quest smoke, and a freshly generated workspace/application check. This qualifies the recorded development increment. Original full G2 qualification, general replacement and prevention, broader trigger programs, full-snapshot release handoff, production Commander browser/XR sessions and physical headset verification remain unfinished.
